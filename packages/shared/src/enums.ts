export enum API_URLS {
    Users = '/api/users',
    Tickets = '/api/tickets',
    Notes = '/api/notes',
}

export enum TicketType {
    Tires = 'Tires',
    Engine = 'Engine',
    FuelPump = 'Fuel Pump',
    FrontWing = 'Front Wing',
    RearWing = 'Rear Wing',
}

export enum TicketStatus {
    Submitted = 'Submitted',
    Open = 'Open',
    Closed = 'Closed',
}
