{
  "name": "root",
  "proxy": "http://localhost:5000",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.5.1"
  },
  "scripts": {
    "change to start on deploy": "npm run serve --prefix packages/backend",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npx lerna bootstrap && npm run build --prefix packages/frontend"
  },
  "dependencies": {}
}